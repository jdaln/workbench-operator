apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: workbenches.default.chorus-tre.ch
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
  {{- include "workbench-operator.labels" . | nindent 4 }}
spec:
  group: default.chorus-tre.ch
  names:
    kind: Workbench
    listKind: WorkbenchList
    plural: workbenches
    singular: workbench
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.server.version
      name: Version
      type: string
    - jsonPath: .status.serverDeployment.serverPod.status
      name: Server-Health
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Workbench is the Schema for the workbenches API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkbenchSpec defines the desired state of Workbench
            properties:
              apps:
                additionalProperties:
                  description: WorkbenchApp defines one application running in the workbench.
                  properties:
                    image:
                      description: |-
                        Image specifies the container image for this app.
                        Registry and Repository are optional and will fall back to operator defaults if not specified.
                        Tag is required.
                      properties:
                        registry:
                          description: Registry represents the hostname of the registry.
                            E.g. quay.io
                          type: string
                        repository:
                          description: Repository contains the image name. E.g. apps/myapp
                          type: string
                        tag:
                          default: latest
                          description: Tag contains the version identifier.
                          pattern: '[a-zA-Z0-9_][a-zA-Z0-9_\-\.]*'
                          type: string
                      type: object
                    kioskConfig:
                      description: KioskConfig holds kiosk-specific configuration
                      properties:
                        jwtToken:
                          description: JWTToken is a short lived jwt used to authenticate
                            to the JWTURL
                          type: string
                        jwtUrl:
                          description: JWTURL is the URL to refresh the short lived
                            JWT token
                          pattern: ^https://.*
                          type: string
                        url:
                          description: URL to load in the kiosk browser
                          pattern: ^https://.*
                          type: string
                      required:
                      - url
                      type: object
                    name:
                      description: Name is the application name (likely its OCI image
                        name as well)
                      maxLength: 30
                      minLength: 1
                      pattern: '[a-zA-Z0-9_][a-zA-Z0-9_\-\.]*'
                      type: string
                    resources:
                      description: |-
                        Resources describes the compute resource requirements.
                        Add anything you'd like to configure. E.g. resources, (App data) volume, etc.
                      properties:
                        claims:
                          description: |-
                            Claims lists the names of resources, defined in spec.resourceClaims,
                            that are used by this container.
  
                            This field depends on the
                            DynamicResourceAllocation feature gate.
  
                            This field is immutable. It can only be set for containers.
                          items:
                            description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                            properties:
                              name:
                                description: |-
                                  Name must match the name of one entry in pod.spec.resourceClaims of
                                  the Pod where this field is used. It makes that resource available
                                  inside a container.
                                type: string
                              request:
                                description: |-
                                  Request is the name chosen for a request in the referenced claim.
                                  If empty, everything from the claim is made available, otherwise
                                  only the result of this request.
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        limits:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Limits describes the maximum amount of compute resources allowed.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                        requests:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Requests describes the minimum amount of compute resources required.
                            If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                            otherwise to an implementation-defined value. Requests cannot exceed Limits.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                      type: object
                    shmSize:
                      anyOf:
                      - type: integer
                      - type: string
                      description: ShmSize defines the size of the required extra /dev/shm
                        space.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    state:
                      default: Running
                      description: |-
                        State defines the desired state
                        Valid values are:
                        - "Running" (default): application is running
                        - "Stopped": application has been stopped
                        - "Killed": application has been force stopped
                      enum:
                      - Running
                      - Stopped
                      - Killed
                      type: string
                  required:
                  - image
                  - name
                  type: object
                description: Apps represent a map of applications any their state
                type: object
              imagePullSecrets:
                description: ImagePullSecrets is the secret(s) needed to pull the image(s).
                items:
                  minLength: 1
                  type: string
                type: array
              initContainer:
                description: InitContainer represents the configuration of the init
                  container.
                properties:
                  version:
                    default: latest
                    description: Version defines the version to use for the app-init
                      container.
                    type: string
                type: object
              server:
                description: Server represents the configuration of the server part.
                properties:
                  clipboard:
                    default: disabled
                    description: |-
                      Clipboard defines the clipboard direction between the workbench and local machine.
                      Options: disabled (default), to-server, to-client, both
                    enum:
                    - disabled
                    - to-server
                    - to-client
                    - both
                    type: string
                  initialResolutionHeight:
                    description: InitialResolutionHeight defines the initial resolution
                      height of the Xpra server.
                    type: integer
                  initialResolutionWidth:
                    description: InitialResolutionWidth defines the initial resolution
                      width of the Xpra server.
                    type: integer
                  resources:
                    description: |-
                      Resources describes the compute resource requirements for the server container.
                      When specified, overrides operator-level defaults.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  user:
                    default: chorus
                    description: User defines the username for the workbench server.
                    type: string
                  userid:
                    default: 1001
                    description: UserID defines the user ID for the workbench server.
                    minimum: 1001
                    type: integer
                  version:
                    default: latest
                    description: Version defines the version to use for the xpra server.
                    type: string
                type: object
              serviceAccountName:
                default: default
                description: Service Account to be used by the pods.
                type: string
              storage:
                description: Storage defines storage mount configuration for S3 and
                  NFS
                properties:
                  local:
                    default: false
                    description: |-
                      Local enables local storage mounting for development at /mnt/workspace-local
                      The init container creates a symlink from /home/{user}/workspace-local to this mount point
                      This is only available when the operator is started with --local-storage-enabled flag
                    type: boolean
                  nfs:
                    default: true
                    description: |-
                      NFS enables NFS storage mounting at /mnt/workspace-scratch
                      The init container creates a symlink from /home/{user}/workspace-scratch to this mount point
                    type: boolean
                  s3:
                    default: true
                    description: |-
                      S3 enables S3 storage mounting via JuiceFS at /mnt/workspace-archive
                      The init container creates a symlink from /home/{user}/workspace-archive to this mount point
                    type: boolean
                type: object
            type: object
          status:
            description: WorkbenchStatus defines the observed state of Workbench
            properties:
              apps:
                additionalProperties:
                  description: WorkbenchStatusappStatus informs about the state of the
                    apps.
                  properties:
                    message:
                      description: Message provides additional context about the status
                      type: string
                    revision:
                      description: Revision is the values of the "deployment.kubernetes.io/revision"
                        metadata.
                      type: integer
                    status:
                      description: Status informs about the real state of the app.
                      enum:
                      - Unknown
                      - Running
                      - Complete
                      - Progressing
                      - Failed
                      - Stopped
                      - Killed
                      type: string
                  required:
                  - revision
                  - status
                  type: object
                type: object
              observedGeneration:
                format: int64
                type: integer
              serverDeployment:
                description: WorkbenchStatusServer represents the server status.
                properties:
                  revision:
                    description: Revision is the values of the "deployment.kubernetes.io/revision"
                      metadata.
                    type: integer
                  serverPod:
                    description: ServerPod provides health information for the server
                      pod
                    properties:
                      message:
                        description: Message provides additional context about the status
                        type: string
                      ready:
                        description: Ready indicates if all containers are ready
                        type: boolean
                      restartCount:
                        description: RestartCount shows container restart count
                        format: int32
                        type: integer
                      status:
                        description: Status represents the current status of the server
                          pod
                        enum:
                        - Waiting
                        - Starting
                        - Ready
                        - Failing
                        - Restarting
                        - Terminating
                        - Terminated
                        - Unknown
                        type: string
                    required:
                    - ready
                    - restartCount
                    - status
                    type: object
                  status:
                    description: Status informs about the real state of the app.
                    enum:
                    - Running
                    - Progressing
                    - Failed
                    type: string
                required:
                - revision
                - status
                type: object
            required:
            - observedGeneration
            - serverDeployment
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
